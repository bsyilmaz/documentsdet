# Ege Karaurgan — 229910141

## Responsibilities

### 1. Experiment 1: Activation Function Comparison (Main Experiment — Depth)
- Controlled experiment design: all parameters held constant, only activation function varies
- Implementation of Sigmoid, Tanh, ReLU, and Leaky ReLU activation functions
- Training for 15 epochs and recording performance metrics
- Plotting and interpreting train loss, test loss, and test accuracy curves

### 2. Model Architecture Design
- Determination of feedforward neural network structure (784 → 256 → 128 → 10)
- Writing the `build_model()` function (parametric: dropout, batchnorm, activation options)
- Implementation of Softmax + CrossEntropyLoss pairing

### 3. Training Infrastructure
- Writing the `train_model()` training loop
- Creating `plot_results()` and `summary_table()` visualization functions

## How It Was Delivered
- Model was built modularly using `nn.Sequential`
- Training loop followed the forward pass → loss → backward pass → optimizer step pipeline
- Experiment 1 results served as the baseline reference for all subsequent experiments
